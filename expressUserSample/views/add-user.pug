extends layouts/main-layout.pug

block content 
    form.user-form(action="/admin/add-user" method="post")
        .form-control 
            label.form-label(for="userName") User Name
            input.form-input(type="text" name="userName")
        button.btn(type="submit") Add New User